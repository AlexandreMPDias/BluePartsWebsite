/*
    function func(row){
        let part = new Part(row);
        part.changeState();
        part.paint();
        console.log(history);
        history.add(part);
    }
    
myHtml.table.addEventListener("click", function (event) {
    event.preventDefault();
    if(event.target.parentNode.id != "part-table" && event.target.id != "part-table"){
        func(event.target.parentNode);
    }
});
*/
